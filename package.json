{
  "name": "westcoast-education",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch db.json --port 3000", "test": "vitest", "test-ui": "vitest --ui"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
